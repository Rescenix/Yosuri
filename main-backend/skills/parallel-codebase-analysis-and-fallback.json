{
  "name": "parallel-codebase-analysis-and-fallback",
  "description": "当需要并行调研多个代码文件但子代理可能失败时，先并行派发任务，若子代理超时或失败则立即回退到直接读取文件并分段提取关键信息以完成汇总。",
  "steps": [
    "并行派发多个子代理任务，分别调研不同文件或模块",
    "监控子代理执行状态，识别失败或超时的任务",
    "对失败的子代理任务，直接使用 read_file 工具分段读取对应文件内容",
    "从读取的文件内容中提取关键信息（如API列表、路由顺序等）",
    "汇总所有成功子代理和直接读取的结果，生成最终结论"
  ]
}