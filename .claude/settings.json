{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\"file_path\".*memory.*\\.md'; then bash c:/Pro2026/re0/Prism/scripts/prismd-bridge.sh create \"$(echo '$CLAUDE_TOOL_INPUT' | python -c 'import json,sys; print(json.load(sys.stdin).get(\"file_path\",\"\"))' 2>/dev/null)\" 2>&1 | sed 's/^/[prismd] /'; fi"
          }
        ]
      }
    ]
  }
}
