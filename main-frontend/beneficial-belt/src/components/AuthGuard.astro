---
// 该组件在客户端执行，检查登录状态
---
<!-- <script>
  (function() {
    const currentPath = window.location.pathname;
    // 允许访问生命线页面
    if (currentPath === '/timeline') return;

    // 检查是否已登录（通过 token 判断）
    const token = localStorage.getItem('token');
    if (!token) {
      // 未登录，重定向到生命线
      window.location.href = '/timeline';
    }
  })();
</script> -->